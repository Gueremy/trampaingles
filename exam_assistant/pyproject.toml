[project]
name = "exam-assistant"
version = "0.1.0"
description = "Windows exam assistant with screen, audio and AI helpers."
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
